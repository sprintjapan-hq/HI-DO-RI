# v0.6 本番環境テスト記録

## テスト環境
本番環境（HI-DO-RI）

## テスト日時
（デプロイ完了後に実施）

## テスト対象
`/feature-history` ページ

## テストケース

### TC1: v0.6カードの表示位置
**手順**:
1. 本番環境の `/feature-history` にアクセス
2. ハードコードされたリリースカードの表示順序を確認

**期待結果**:
- v0.6が最初に表示される
- v0.5が2番目に表示される
- v0.4、v0.3、v0.2、v0.1の順序が保持されている

**結果**: （本番デプロイ後に確認）

### TC2: v0.6カードの内容正確性
**手順**:
1. v0.6カードのすべてのフィールドを確認

**期待結果**:
- バージョン: `v0.6` が表示される
- 日付: `2026年9月12日` が表示される
- 時刻: `18:15` が表示される
- タイトル: `日本語条件からの候補日時の解釈を改善` が表示される
- 機能リスト:
  - `「2026/9/18-9/22」のような日付範囲を、今後30日に広げず指定期間だけに限定`
  - `「午後」を13:00〜18:00として解釈`
  - `「13:00-15:00」のようなコロン付き時刻範囲をそのまま採用`
  - `期間や時間帯が読み取れたときは、毎日19:00〜の初期値へ戻さない`

**結果**: （本番デプロイ後に確認）

### TC3: 過去バージョンの保全確認
**手順**:
1. v0.5カードの内容を確認
2. v0.1〜v0.4のカードを確認

**期待結果**:
- v0.5のタイトル、日付、時刻、機能リストが変更されていない
- v0.1〜v0.4のすべてのフィールドが変更されていない

**結果**: （本番デプロイ後に確認）

### TC4: デスクトップ表示
**手順**:
1. デスクトップブラウザでページを表示
2. すべてのカードのレイアウトを確認

**期待結果**:
- v0.6カードが他のカードと同じスタイルで表示される
- カードの境界線、影、パディングが適切
- テキストの折り返しが正常

**結果**: （本番デプロイ後に確認）

### TC5: モバイル表示
**手順**:
1. モバイルブラウザまたはデベロッパーツールのモバイルビューでページを表示
2. レスポンシブレイアウトを確認

**期待結果**:
- v0.6カードがモバイル画面に適切にフィットする
- 日付と時刻の表示が折り返しまたは適切に配置される
- タイトルと機能リストが読みやすく表示される

**結果**: （本番デプロイ後に確認）

### TC6: ページパフォーマンス
**手順**:
1. ページの読み込み速度を確認
2. ブラウザのコンソールでエラーがないことを確認

**期待結果**:
- ページが正常に読み込まれる
- JavaScriptエラーが発生しない
- カードの追加によるパフォーマンス劣化がない

**結果**: （本番デプロイ後に確認）

### TC7: 自動公開リリースとの統合
**手順**:
1. `/api/feature-requests/releases` エンドポイントの動作を確認
2. 自動公開アイテムがある場合、表示順序を確認

**期待結果**:
- 自動公開アイテムがハードコードされたカードより前に表示される
- v0.6が自動公開アイテムの後、ハードコードされたカードの先頭に表示される

**結果**: （本番デプロイ後に確認）

### TC8: ダークモード対応
**手順**:
1. ライトモードで表示を確認
2. ダークモードに切り替えて表示を確認

**期待結果**:
- 両モードでv0.6カードが適切に表示される
- テキストの可読性が保たれる
- カードの背景と境界線が適切に表示される

**結果**: （本番デプロイ後に確認）

## テスト結果サマリー
（全テストケース実施後に記入）

## 検出された問題
（問題が見つかった場合に記入）

## フォローアップアクション
（必要に応じて記入）
